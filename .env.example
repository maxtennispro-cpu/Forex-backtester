# Copy to .env (in the project root) and fill in. Never commit the real .env.
# Practice-account token: OANDA dashboard -> Manage API Access
OANDA_API_KEY=your-practice-api-token
OANDA_ENVIRONMENT=practice
